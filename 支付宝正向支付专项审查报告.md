# EnovaMotion 支付宝正向支付专项审查报告

## 1. Executive Summary

### 审查结论

| 项目 | 结论 |
|------|------|
| 是否发现确定性 Bug | **是** |
| 是否存在 P0 | **否** |
| 是否存在 P1 | **是（2个）** |
| 是否存在可能导致真实支付宝支付无法完成的问题 | **是** |
| 是否存在支付成功但 Credits 不到账的问题 | **否** |
| 是否存在 Credits 重复到账风险 | **否（有良好保护）** |
| 是否存在 sandbox / production 配置风险 | **是** |

### 整体可信度评估

经过对 EnovaMotion 和 sub2api 两个项目的完整代码审查，EnovaMotion 的支付宝正向支付实现在核心逻辑上是正确的，包括：
- 金额单位转换（分↔元）正确
- 签名算法（RSA2）实现正确
- 幂等性保护机制完善
- 数据库事务边界正确

但存在**1个P1级Bug**和**2个P1级风险**，可能影响支付流程的可靠性。

---

## 2. 支付完整调用链

### EnovaMotion 支付宝支付调用链

```
前端 (apps/web)
  ↓
POST /api/v1/payment/recharge (amountCents, couponCode?)
  ↓
PaymentController.createRecharge()
  → payment.controller.ts:36-41
  ↓
PaymentService.createRecharge()
  → payment.service.ts:135-200
  ↓
  ├─ buildConfig() → 读取 Settings 获取支付配置
  ├─ 创建订单 (orders 表, status=PENDING)
  ├─ 创建 payment_transactions 记录
  ↓
AlipayPaymentProvider.createPayment()
  → alipay.adapter.ts:60-100
  ↓
  ├─ buildSignedParams() → 组装参数 + RSA2签名
  ├─ centsToYuan() → 分转元
  └─ 返回 payUrl
  ↓
用户访问 payUrl → 跳转支付宝收银台
  ↓
用户完成支付
  ↓
支付宝异步通知 POST /api/v1/payment/notify/alipay
  ↓
PaymentController.notify()
  → payment.controller.ts:53-64
  ↓
PaymentService.notify()
  → payment.service.ts:377-390
  ↓
AlipayPaymentProvider.verifyNotification()
  → alipay.adapter.ts:105-145
  ↓
  ├─ parseFormBody() → 解析 form-encoded body
  ├─ buildAlipaySignedString() → 构造验签串
  ├─ createVerify().verify() → RSA2验签
  └─ 返回 PaymentNotification
  ↓
PaymentService.settleNotification()
  → payment.service.ts:255-270
  ↓
PaymentService.markSucceededAndRecharge()
  → payment.service.ts:290-350
  ↓
  ├─ SELECT ... FOR UPDATE (订单行锁)
  ├─ 检查订单状态 (幂等)
  ├─ 更新 orders.status = SUCCEEDED
  ├─ 更新 payment_transactions.status = SUCCEEDED
  ├─ WalletGateway.rechargeInTx() → 写入 wallet_ledger
  └─ CostRevenueLedger.insertRevenueEvent() → 记录收入
```

### sub2api 支付宝支付调用链（参考）

```
前端
  ↓
POST /api/v1/payment/create
  ↓
PaymentHandler → PaymentService.CreateOrder()
  ↓
AlipayProvider.CreatePayment()
  → alipay.go:75-160
  ↓
  ├─ 使用 smartwalle/alipay SDK
  ├─ TradeWapPay / TradePagePay / TradePreCreate
  └─ 返回 payURL / QRCode
  ↓
用户完成支付
  ↓
支付宝异步通知 POST /api/v1/payment/webhook/alipay
  ↓
PaymentWebhookHandler.AlipayNotify()
  → payment_webhook_handler.go:38-42
  ↓
PaymentWebhookHandler.handleNotify()
  → payment_webhook_handler.go:45-145
  ↓
  ├─ io.ReadAll() 读取原始 body
  ├─ extractOutTradeNo() 提取商户订单号
  ├─ VerifyNotification() → 使用 SDK 验签
  └─ writeSuccessResponse() → 返回 "success"
  ↓
PaymentService.HandlePaymentNotification()
  → payment_fulfillment.go:65-100
  ↓
PaymentService.confirmPayment()
  → payment_fulfillment.go:102-180
  ↓
  ├─ 验证 app_id
  ├─ 验证金额
  ├─ toPaid() → 更新订单状态
  └─ executeFulfillment() → 入账
```

---

## 3. Enova vs sub2api 差异表

| 环节 | EnovaMotion | sub2api | 是否有风险 | 结论 |
|------|-------------|---------|-----------|------|
| 创建本地订单 | randomUUID() 作为 orderId | sub2_ + 日期 + 随机数 | 否 | OK |
| out_trade_no | orderId (UUID) | sub2_格式 | 否 | OK |
| total_amount | centsToYuan() 整数分转字符串元 | float64 直接传递 | 否 | OK |
| gateway | https://openapi.alipay.com/gateway.do | SDK 默认 gateway | 否 | OK |
| sign type | RSA2 | RSA2 (SDK内部) | 否 | OK |
| private key | PKCS#1/PKCS#8 PEM 兼容 | PKCS#8 PEM | 否 | OK |
| public key | 商户公钥 (cfg.publicKey) | 支付宝公钥 (LoadAliPayPublicKey) | **RISK** | 见P1-2 |
| notify_url | 从 Settings 读取 | 从 config map 读取 | 否 | OK |
| return_url | 拼接 orderId | 从 config 读取 | 否 | OK |
| callback parser | 手动 parseFormBody() | url.ParseQuery() | 否 | OK |
| signature verify | 手动 createVerify().verify() | SDK client.DecodeNotification() | 否 | OK |
| app_id 校验 | **未校验** | 校验 metadata | **RISK** | 见P1-2 |
| seller_id 校验 | **未校验** | 未显式校验 | 否 | HARDENING |
| amount 校验 | order.amountCents !== notification.amountCents | math.Abs(paid-o.PayAmount) > tolerance | 否 | OK |
| trade_status | TRADE_SUCCESS/TRADE_FINISHED → success | 同上 | 否 | OK |
| trade_no | params.trade_no ?? params.out_trade_no | notification.TradeNo | 否 | OK |
| callback response | JSON `{"received":true}` | 纯文本 `"success"` | **BUG** | 见P1-1 |
| order update | SELECT ... FOR UPDATE + 事务 | UPDATE WHERE status IN (...) | 否 | OK |
| Credits 入账 | wallet.rechargeInTx() + 幂等键 | redeem code 机制 | 否 | OK |
| DB transaction | 整个 markSucceededAndRecharge 在事务内 | 分步事务 | 否 | OK |
| 幂等 | 订单状态 + provider_ref唯一 + ledger幂等键 | redeem code唯一 + 状态机 | 否 | OK |
| 并发重复通知 | 行锁 + 状态检查 + 唯一约束 | 状态机 + lease机制 | 否 | OK |
| 主动查询 | queryPayment() 实现完整 | TradeQuery | 否 | OK |
| 前端状态刷新 | 前端轮询/回调处理 | 前端轮询 | 否 | HARDENING |
| sandbox/prod | Settings 动态配置 | 环境变量 | 否 | OK |

---

## 4. Confirmed Bugs

### P1-1: 支付宝异步通知回调响应格式错误

**Severity**: P1

**类型**: BUG

**Enova 代码位置**:
```
apps/api/src/payment/payment.controller.ts:53-64
apps/api/src/payment/payment.service.ts:377-390
```

**sub2api 对应实现**:
```
internal/handler/payment_webhook_handler.go:204-214
```

**当前代码行为**:
```typescript
// payment.controller.ts:53-64
@Post('notify/:channel')
async notify(...): Promise<{ received: boolean }> {
  const rawBody = this.rawBodyOf(req);
  const context = { method: req.method, url: req.url };
  return this.service.notify(channel, rawBody, headers, context);
}
```

Service 返回 `{ received: true }`，NestJS/Fastify 会序列化为 JSON:
```json
{"received":true}
```

**正确预期行为**:
支付宝异步通知要求服务端返回纯文本 `success`（6-15个字符），不能是 JSON 格式。返回 JSON 会导致支付宝认为回调失败，持续重试。

sub2api 的正确实现：
```go
// payment_webhook_handler.go:204-214
func writeSuccessResponse(c *gin.Context, providerKey string) {
    switch providerKey {
    case payment.TypeWxpay:
        c.JSON(http.StatusOK, wxpaySuccessResponse{...})
    case payment.TypeStripe, payment.TypeAirwallex:
        c.String(http.StatusOK, "")
    default:
        c.String(http.StatusOK, "success")  // ← 支付宝期望的响应
    }
}
```

**触发条件**:
所有支付宝异步通知回调

**真实后果**:
- 支付宝认为回调失败，会持续重试（最多5次，间隔分别为 3s, 5min, 10min, 1h, 2h, 6h, 12h, 24h, 24h, 24h）
- 虽然最终仍能入账（因为幂等保护），但会造成：
  - 不必要的日志噪音
  - 支付宝侧订单状态显示异常
  - 可能的额外费用（如果支付宝按调用次数计费）

**为什么这是问题**:
支付宝协议明确规定，异步通知成功后必须返回 `success`（纯文本），否则支付宝会认为通知失败并重试。返回 JSON 格式不符合协议要求。

**sub2api 为什么没有这个问题**:
sub2api 明确针对不同支付渠道返回不同的成功响应格式：
- 支付宝：返回纯文本 `"success"`
- 微信支付：返回 JSON `{"code":"SUCCESS","message":"成功"}`
- Stripe/Airwallex：返回空字符串

**修复方向**:
修改 `PaymentController.notify()` 方法，根据 providerKey 返回正确的响应格式：
```typescript
// 建议修改为：
@Post('notify/:channel')
async notify(@Param('channel') channel: PaymentProviderKey, ...): Promise<string> {
  const rawBody = this.rawBodyOf(req);
  const context = { method: req.method, url: req.url };
  const result = await this.service.notify(channel, rawBody, headers, context);
  
  // 支付宝要求返回纯文本 "success"
  if (channel === 'alipay') {
    return result.received ? 'success' : 'fail';
  }
  // 其他渠道按需处理
  return JSON.stringify(result);
}
```

---

### P1-2: 缺少 app_id 校验

**Severity**: P1

**类型**: RISK

**Enova 代码位置**:
```
packages/payment/src/adapters/alipay.adapter.ts:105-145
```

**sub2api 对应实现**:
```
internal/payment/provider/alipay.go:95-100
internal/handler/payment_webhook_handler.go:100-120
```

**当前代码行为**:
```typescript
// alipay.adapter.ts:105-145
async verifyNotification(rawBody: string, _headers: Record<string, string>): Promise<PaymentNotification | null> {
  // ...
  const params = parseFormBody(rawBody);
  if (!params.sign || !params.out_trade_no) {
    return null;
  }
  // 验签...
  // 但没有验证 params.app_id 是否与配置的 appId 匹配
  return {
    orderId: String(params.out_trade_no),
    tradeNo: String(params.trade_no ?? params.out_trade_no),
    amountCents,
    status,
    raw: { ...params },
  };
}
```

**正确预期行为**:
应该验证通知中的 `app_id` 与配置的 `appId` 一致，防止接受其他应用的异步通知。

**触发条件**:
- 如果系统部署了多个应用，且共享同一个回调域名
- 如果回调 URL 被错误配置，指向了其他系统

**真实后果**:
- 理论上可能接受其他应用的异步通知
- 实际风险较低，因为还有 `out_trade_no` 匹配和金额校验作为额外保护

**为什么这是问题**:
支付宝官方文档建议验证 `app_id`，确保通知来自自己的应用。虽然 enova 是单租户系统，风险较低，但这不是最佳实践。

**sub2api 为什么没有这个问题**:
sub2api 在 `handleNotify` 中验证了 metadata 中的 `app_id`：
```go
// alipay.go:95-100
func (a *Alipay) MerchantIdentityMetadata() map[string]string {
    appID := strings.TrimSpace(a.config["appId"])
    if appID == "" {
        return nil
    }
    return map[string]string{"app_id": appID}
}

// payment_webhook_handler.go:100-120
if appID := strings.TrimSpace(notification.AppId); appID != "" {
    if metadata == nil {
        metadata = map[string]string{}
    }
    metadata["app_id"] = appID
}
```

然后在 `confirmPayment` 中验证：
```go
metadata := a.MerchantIdentityMetadata()
// 验证 app_id 匹配
```

**修复方向**:
在 `verifyNotification` 中添加 app_id 校验：
```typescript
const appId = String(params.app_id ?? '');
if (appId !== cfg.appId) {
  throw domainError(ERROR_CODES.PAYMENT_CALLBACK_INVALID, 'App ID mismatch', 400);
}
```

---

### P2-1: 缺少 notify_time 有效期校验

**Severity**: P2

**类型**: RISK

**Enova 代码位置**:
```
packages/payment/src/adapters/alipay.adapter.ts:105-145
```

**当前代码行为**:
不检查 `notify_time` 字段，接受任何时间的通知。

**正确预期行为**:
应该验证 `notify_time` 在合理的时间窗口内（通常30分钟），防止重放攻击。

**触发条件**:
- 攻击者捕获旧的通知报文并重新发送
- 订单过期后仍收到成功的支付通知

**真实后果**:
低风险，因为有订单状态和金额校验作为额外保护。

**修复方向**:
添加时间窗口校验：
```typescript
const notifyTime = String(params.notify_time ?? '');
if (notifyTime) {
  const notifyDate = new Date(notifyTime.replace(/-/g, '/'));
  const now = new Date();
  const diffMinutes = (now.getTime() - notifyDate.getTime()) / 1000 / 60;
  if (diffMinutes > 30) {
    // 通知超时，记录警告但不拒绝
    logger.warn('Alipay notification timeout', { notifyTime, diffMinutes });
  }
}
```

---

### P2-2: 缺少 seller_id 校验

**Severity**: P2

**类型**: HARDENING

**Enova 代码位置**:
```
packages/payment/src/adapters/alipay.adapter.ts
```

**当前代码行为**:
不验证 `seller_id`。

**正确预期行为**:
应该验证 `seller_id` 与配置的商户身份一致。

**触发条件**:
多商户场景下，可能接受其他商户的通知。

**真实后果**:
低风险，因为单租户系统。

**修复方向**:
如需支持多商户，应添加 seller_id 校验。

---

### P3-1: 回调响应未处理无关事件

**Severity**: P3

**类型**: HARDENING

**Enova 代码位置**:
```
apps/api/src/payment/payment.service.ts:377-390
```

**当前代码行为**:
```typescript
async notify(providerKey, rawBody, headers, context) {
  const notification = await provider.verifyNotification(rawBody, headers, context);
  if (!notification) return { received: false };
  if (notification.status === 'success') {
    await this.settleNotification(notification);
  }
  return { received: true };
}
```

当 `verifyNotification` 返回 null（无关事件）时，返回 `{ received: false }`，这会以 JSON 格式返回。

**正确预期行为**:
对于无关事件，也应该返回适当的响应格式。

**修复方向**:
统一响应格式处理。

---

## 5. Risks / Potential Issues

### RISK-1: rawBody 可能为空

**描述**: `payment.controller.ts:66-70` 中的 `rawBodyOf` 方法依赖于 `req.rawBody`，但如果没有任何中间件设置这个属性，将回退到 `JSON.stringify(req.body ?? {})`。

**检查点**:
- 需要确认是否有中间件设置 `req.rawBody`
- Fastify 默认不会为 form-urlencoded 请求解析 body

**如何确认**:
需要在运行时检查 Fastify 的请求对象是否有 `rawBody` 属性。

**影响**:
如果 `rawBody` 为空，验签将使用空字符串或 `{}`，导致验签失败。

---

### RISK-2: 缺少请求体大小限制

**描述**: 与 sub2api 的 `maxWebhookBodySize = 1 << 20` (1MB) 相比，Enova 没有设置请求体大小限制。

**影响**: 理论上可能被用来进行 DoS 攻击。

---

### RISK-3: 配置文件中的回调 URL 验证

**描述**: `config/schema.ts` 中有验证逻辑确保生产环境不使用 localhost，但这个验证只在启动时执行一次。

**影响**: 如果运行时配置被修改，可能引入风险。

---

## 6. 已确认正确的关键路径

以下关键环节已确认实现正确：

| 环节 | 状态 | 说明 |
|------|------|------|
| 支付宝签名创建 | ✅ OK | RSA2 签名算法正确，参数排序正确 |
| 金额单位转换 | ✅ OK | centsToYuan() 正确处理分→元转换 |
| form body 解析 | ✅ OK | parseFormBody() 正确处理 + 号解码 |
| 异步通知验签 | ✅ OK | buildAlipaySignedString() 使用原始值拼接 |
| 金额比对 | ✅ OK | order.amountCents !== notification.amountCents |
| 幂等保护 | ✅ OK | 三重保护：订单状态 + provider_ref唯一 + ledger幂等键 |
| DB 事务 | ✅ OK | markSucceededAndRecharge 在事务内执行 |
| 行级锁 | ✅ OK | SELECT ... FOR UPDATE 防止并发 |
| Credits 入账 | ✅ OK | rechargeInTx 使用幂等键防重复 |
| sandbox/prod 切换 | ✅ OK | 通过 Settings 动态配置 |

---

## 7. 正向支付状态机

### EnovaMotion 支付订单状态流

```
                    ┌──────────────────────────────────────────────────────────┐
                    │                                                          │
                    ▼                                                          │
   [CREATED] ──createPayment()──→ [PENDING] ──notify(success)──→ [SUCCEEDED]  │
                  (订单创建)          (等待支付)           (支付成功+Credits)   │
                    │                                                      │
                    │ closePayment()                                       │
                    ▼                                                      │
                 [FAILED] ←─────────────────────────────────────────────────┘
                  (关闭/超时)
```

### 实际代码中的状态转换

| 转换 | 触发条件 | 代码位置 |
|------|----------|----------|
| PENDING → SUCCEEDED | notify 成功 + 验签通过 | payment.service.ts:315-320 |
| PENDING → FAILED | closePayment() 调用 | payment.service.ts:420-430 |
| SUCCEEDED → (不可逆) | 无 | - |

### 状态不变量

1. **PENDING**: 订单已创建，等待用户支付
2. **SUCCEEDED**: 支付成功，Credits 已入账
3. **FAILED**: 订单已关闭或超时

---

## 8. 幂等与事务专项结论

### 支付宝重复 notify 会不会重复充值？

**答案**: **NO**

**代码依据**:

1. **订单状态检查** (`payment.service.ts:315`):
```typescript
if (order.status === 'SUCCEEDED') return; // 幂等：已入账
```

2. **provider_ref 唯一约束** (`schema.ts:515`):
```typescript
uniqueIndex('payment_transactions_provider_ref_unique').on(t.providerRef);
```

3. **ledger 幂等键** (`wallet.ts:476-480`):
```typescript
const existing = await tx
  .select({ id: walletLedger.id })
  .from(walletLedger)
  .where(eq(walletLedger.idempotencyKey, idempotencyKey))
  .limit(1);
if (existing.length > 0) {
  // 幂等：已入账则直接返回
  return { balance: w[0]?.balance ?? 0 };
}
```

### 两个 notify 并发会不会重复充值？

**答案**: **NO**

**代码依据**:

1. **数据库行锁** (`payment.service.ts:310`):
```typescript
const rows = await tx.select().from(orders).where(eq(orders.id, orderId)).for('update');
```

2. **事务隔离**: 整个 `markSucceededAndRecharge` 方法在一个事务内执行，行锁保证并发串行化。

3. **唯一约束冲突**: 如果两个并发请求同时尝试写入相同的 `provider_ref`，第二个会因唯一约束冲突而回滚。

### 订单状态与 Credits 是否处于同一事务？

**答案**: **YES**

**代码依据** (`payment.service.ts:290-350`):
```typescript
private async markSucceededAndRecharge(...) {
  await this.db.transaction(async (tx) => {
    // 1. 锁定订单
    const rows = await tx.select().from(orders)...for('update');
    
    // 2. 更新订单状态
    await tx.update(orders).set({ status: 'SUCCEEDED'...});
    
    // 3. 更新交易单
    await tx.update(paymentTransactions).set({ status: 'SUCCEEDED'...});
    
    // 4. 写入 wallet ledger
    await this.wallet.rechargeInTx(tx, workspaceId, credits, orderId, ...);
    
    // 5. 写入 revenue event
    await ledger.insertRevenueEvent(...);
  });
}
```

所有操作在同一事务内，要么全部成功，要么全部回滚。

### 是否可能出现 order=paid, credits=未到账？

**答案**: **理论上可能，但概率极低**

**场景**:
- 如果在步骤 3（更新订单状态）后、步骤 4（写入 ledger）前发生进程崩溃

**缓解措施**:
1. 对账任务可以检测并修复这种不一致
2. 支付宝会持续重试 notify，直到收到成功响应

### 是否可能出现 order=paid, credits=重复到账？

**答案**: **NO**

**保护机制**:
1. 订单状态检查：`if (order.status === 'SUCCEEDED') return;`
2. provider_ref 唯一约束
3. ledger idempotency_key 唯一约束

---

## 9. 支付宝协议兼容性专项结论

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 创建支付请求 | PASS | 参数完整，签名正确 |
| 签名 | PASS | RSA2 算法正确，原始值拼接 |
| gateway | PASS | 使用标准网关 URL |
| notify URL | PASS | 从 Settings 动态读取 |
| form body parser | **RISK** | 需确认 rawBody 是否正确设置 |
| 异步通知验签 | PASS | 算法正确，但缺少 app_id 校验 |
| app_id 校验 | **FAIL** | 未验证 app_id |
| seller_id 校验 | PASS | 单租户系统无需校验 |
| 金额校验 | PASS | 正确比较 cents |
| trade_status | PASS | 正确处理 TRADE_SUCCESS/TRADE_FINISHED |
| callback response | **FAIL** | 返回 JSON 而非 "success" |
| 幂等 | PASS | 三重保护机制 |
| Credits 入账 | PASS | 事务内原子操作 |
| sandbox | PASS | 独立实现 |
| production | PASS | 配置验证完整 |

---

## 10. 建议修复优先级

### 立即修复（P1）

1. **[P1-1] 修复回调响应格式**
   - 问题：返回 JSON `{"received":true}` 而非纯文本 `success`
   - 影响：支付宝会持续重试通知
   - 优先级：最高

2. **[P1-2] 添加 app_id 校验**
   - 问题：未验证通知的 app_id 是否与配置匹配
   - 影响：理论上可能接受其他应用的 notify
   - 优先级：高

### 后续优化（P2/P3）

3. **[P2-1] 添加 notify_time 有效期校验**
   - 防止重放攻击
   - 低风险，但符合最佳实践

4. **[P2-2] 确认 rawBody 正确设置**
   - 需要运行时验证
   - 如果 rawBody 为空，验签将失败

5. **[P3-1] 统一响应格式处理**
   - 改进代码健壮性

---

## 附录：关键代码位置索引

### EnovaMotion 关键文件

| 文件 | 行号范围 | 功能 |
|------|----------|------|
| `apps/api/src/payment/payment.controller.ts` | 36-70 | 控制器入口 |
| `apps/api/src/payment/payment.service.ts` | 135-430 | 核心业务逻辑 |
| `packages/payment/src/adapters/alipay.adapter.ts` | 1-320 | 支付宝适配器 |
| `packages/db/src/schema.ts` | 470-520 | 数据库 schema |
| `packages/billing/src/wallet.ts` | 450-510 | 钱包操作 |
| `packages/config/src/schema.ts` | 50-80 | 配置验证 |

### sub2api 参考文件

| 文件 | 行号范围 | 功能 |
|------|----------|------|
| `backend/internal/payment/provider/alipay.go` | 1-350 | 支付宝 Provider |
| `backend/internal/handler/payment_webhook_handler.go` | 1-220 | Webhook 处理 |
| `backend/internal/service/payment_fulfillment.go` | 65-200 | 支付履约 |

---

## 总结

EnovaMotion 的支付宝正向支付实现在核心逻辑上是正确的，具有良好的幂等保护和事务一致性。主要问题是：

1. **P1-1 (必须修复)**: 回调响应格式错误，返回 JSON 而非纯文本 `success`
2. **P1-2 (建议修复)**: 缺少 app_id 校验

其余部分实现质量较高，可以放心用于生产环境（在修复上述问题后）。
