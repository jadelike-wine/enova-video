/**
 * 由 OpenAPI 自动生成的类型（`pnpm sdk:generate`）。
 * 运行 API Server 后执行 `pnpm --filter @enova/sdk generate` 生成。
 * 禁止手改本文件。
 */
/* eslint-disable @typescript-eslint/no-explicit-any */
export type GeneratedApi = Record<string, any>;

export * from './http-client.js';